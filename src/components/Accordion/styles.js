import styled from "styled-components";

export const Accordion = styled.strong`
  cursor: pointer;
  display: flex;
  justify-content: space-between;

  > strong {
    color: #999591;
    font-size: 20px;
    display: block;
  }

  > p {
    color: #999591;
  }
`;
